  vector<int> sortedSquares(vector<int>& nums) {
    int n=nums.size();
        for(int i =0;i<n;++i)
        {
            nums[i]=nums[i]*nums[i];
        }
        vector<int> v=nums;
     sort(v.begin(), v.end());
        
        return v;
    }
